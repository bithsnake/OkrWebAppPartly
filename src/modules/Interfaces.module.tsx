
/** interface used for styling rules */
export interface IStylings {
  rectangleStyling: {
    display: string;
    placeItems: string;
    color?: string;
    backgroundColor?: string;
  };
}

export interface Ipages {
    pepWord?: string,
    okrStatus: string,
    okrChart: string,
}