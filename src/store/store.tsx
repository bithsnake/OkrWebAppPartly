import React from 'react';

/*TODO Still need to create a redux store torefactor + centralize code that is reused everywhere to eliminate the need or propdrilling */
const store = () => {
  return <div></div>;
};

export default store;
